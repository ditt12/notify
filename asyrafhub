local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer

local TOKEN = "8538531375:AAHJYgPsvydBm-HTu7_VYVfP17BrUv2c3g8"
local CHAT_ID = "6554802149"

local request =
    (syn and syn.request)
    or http_request
    or request

print("[DEBUG] request =", request)

local function send(msg)
    if not request then
        warn("HTTP request NOT supported")
        return
    end

    local success, err = pcall(function()
        request({
            Url = "https://api.telegram.org/bot" .. TOKEN .. "/sendMessage",
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = HttpService:JSONEncode({
                chat_id = CHAT_ID,
                text = msg
            })
        })
    end)

    print("[DEBUG] send status:", success, err)
end

--// CONNECTED TEST
send("✅ CONNECTED TEST: " .. LocalPlayer.Name)
print("CONNECTED SENT (if HTTP works)")
